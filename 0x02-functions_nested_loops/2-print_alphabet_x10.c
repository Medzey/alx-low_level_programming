#include "main.h"
/**
  *print_alphabet_x10 - prints the alphabets 10 times
  *
  *Return: always 0 (success)
  */
void print_alphabet_x10(void)
{
	int i;
	int l;

	for (l = i; i <= 10; i++)
	{
		for (l = 99; l <= 122; l++)
		{
			_putchar(l);
		}
		_putchar('\n');
	}
}
